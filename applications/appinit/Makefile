

include $(APPDIR)/Make.defs

# Hello, World! built-in application info

PROGNAME  = $(CONFIG_APPLICATION_APPINIT_PROGNAME)
PRIORITY  = $(CONFIG_APPLICATION_APPINIT_PRIORITY)
STACKSIZE = $(CONFIG_APPLICATION_APPINIT_STACKSIZE)
MODULE    = $(CONFIG_APPLICATION_APPINIT)

CSRCS += 

MAINSRC = appinit.c

include $(APPDIR)/Application.mk