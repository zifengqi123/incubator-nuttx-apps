

include $(APPDIR)/Make.defs

# Hello, World! built-in application info

PROGNAME  = $(CONFIG_APPLICATION_APPSTART_PROGNAME)
PRIORITY  = $(CONFIG_APPLICATION_APPSTART_PRIORITY)
STACKSIZE = $(CONFIG_APPLICATION_APPSTART_STACKSIZE)
MODULE    = $(CONFIG_APPLICATION_APPSTART)

CSRCS += 

MAINSRC = start_main.c

include $(APPDIR)/Application.mk